plugins {
    id "com.android.application"
}

android {
    namespace "hk.beckonstars.app"
    compileSdk 34

    defaultConfig {
        applicationId "hk.beckonstars.app"
        minSdk 23
        targetSdk 34
        versionCode (System.getenv("VERSION_CODE") as Integer) ?: 1
        versionName "1.0"
    }
}

dependencies {
    implementation "androidx.core:core:1.13.1"
    implementation "androidx.credentials:credentials:1.3.0"
    implementation "androidx.credentials:credentials-play-services-auth:1.3.0"
    implementation "com.google.android.libraries.identity.googleid:googleid:1.1.1"
}

// 共用 web 核心（repo 根目錄 web/）是唯一事實來源；
// 每次建置前整棵同步進 assets（Sync 會刪除目的地多餘檔案）。
// iOS 殼層以 folder reference 引用同一個 web/ 目錄。
tasks.register("syncWebAssets", Sync) {
    from new File(rootProject.projectDir.parentFile, "web")
    into layout.projectDirectory.dir("src/main/assets")
}

preBuild.dependsOn("syncWebAssets")
